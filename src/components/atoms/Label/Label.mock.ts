import type { LabelInterface } from './Label.interface'


export const LabelMock: LabelInterface = {
  helperText: 'We will only use this to personalize your account.',
  htmlFor: 'first-name',
  required: true,
  size: 'm',
  text: 'First name',
}
